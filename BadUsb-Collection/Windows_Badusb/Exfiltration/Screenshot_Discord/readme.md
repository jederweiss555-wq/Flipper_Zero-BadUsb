# Screenshot to Discord

Takes a full screenshot of the primary screen and uploads it to a Discord webhook.

- **Output:** Screenshot sent directly to Discord channel
- **Requirements:** Internet connection, Discord webhook URL (replace `DISCORD_WEBHOOK_URL`)
- **Note:** Uses `System.Windows.Forms` and `System.Drawing` — no external tools needed
