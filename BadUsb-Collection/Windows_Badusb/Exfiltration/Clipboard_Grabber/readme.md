# Clipboard Grabber

Reads the current clipboard contents and sends them to a Discord webhook.

- **Output:** Clipboard text posted to Discord channel
- **Requirements:** Internet connection, Discord webhook URL (replace `DISCORD_WEBHOOK_URL`)
- **Note:** Only captures text content — images/files in clipboard are ignored
