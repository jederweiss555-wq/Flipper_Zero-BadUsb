# Browser History Dump

Copies Chrome and Edge browsing history SQLite databases and dumps URLs.

- **Output:** `%TEMP%\browser_history.txt`
- **Note:** History files are locked by browser when open — close browser first or use VSS shadow copy
