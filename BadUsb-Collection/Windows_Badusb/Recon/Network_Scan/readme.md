# Network Recon

Collects local network information: ARP table, routing table, network adapters, DNS cache. Output saved to `%TEMP%\net_recon.txt` and opened in Notepad.

- **Privilege required:** None
- **Output:** `%TEMP%\net_recon.txt`
