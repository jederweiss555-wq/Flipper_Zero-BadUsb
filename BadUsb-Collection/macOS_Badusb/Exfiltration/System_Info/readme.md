# macOS System Information

Collects macOS system info (OS version, hardware, network, users, public IP) and copies it to clipboard.

- **Output:** `/tmp/sysinfo.txt` + clipboard
- **Requirements:** None (all built-in commands)
- **Note:** Combine with a Discord/email exfil script to send the data remotely
