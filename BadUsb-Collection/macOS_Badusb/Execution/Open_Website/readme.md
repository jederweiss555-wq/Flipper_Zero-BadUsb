# Open Website (macOS)

Opens a URL in the default browser on macOS using the `open` command.

- **Requirements:** Replace `WEBSITE_URL` with the target URL
- **Note:** Opens in default browser. Can be used for phishing page delivery, rickrolling, or triggering a drive-by download.
